# Python cache and virtual environments
__pycache__/
*.pyc
.pytest_cache/
.mypy_cache/
.ruff_cache/
.venv/
venv/
env/

# Local environment and secrets
.env
.env.*

# Logs and runtime output
app/data/logs/
logs/
*.log

# Local DB artifacts — app/data/app.db và app/data/chroma/ ĐƯỢC commit (deploy
# Render cần sẵn data, không tự re-ingest được trên free tier); chỉ ignore file
# tạm/log của chúng.
app/data/app.db-shm
app/data/app.db-wal
app/data/reports/

# Editor and OS files
.DS_Store
Thumbs.db

# Frontend build artifacts (Frontend/webapp) — built by Docker/CI, not committed
Frontend/webapp/node_modules/
Frontend/webapp/dist/
Frontend/webapp/tsconfig.tsbuildinfo